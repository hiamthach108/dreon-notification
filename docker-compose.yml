version: '3.8'

services:
  dreon-notification:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: dreon-notification-app
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      # App
      APP_NAME: dreon-notification
      APP_VERSION: 1.0.0
      HTTP_HOST: 0.0.0.0
      HTTP_PORT: 8080
      
      # Cache
      CACHE_DEFAULT_EXPIRE_TIME_SEC: 3600
      CACHE_CLEANUP_INTERVAL_HOUR: 24
      
      # Redis
      REDIS_HOST: redis
      REDIS_PORT: 6379
      REDIS_PASSWORD: ""
      REDIS_DB: 0
      
      # PostgreSQL
      POSTGRES_CONNECTION_NAME: ""
      POSTGRES_USERNAME: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_HOST: postgres
      POSTGRES_PORT: 5432
      POSTGRES_DBNAME: dreon-notification
      POSTGRES_SCHEMA: public
      POSTGRES_MAX_IDLE_CONNECTION: 5
      POSTGRES_MAX_OPEN_CONNECTION: 50
      POSTGRES_MAX_LIFE_TIME: 1800
      POSTGRES_SSL: false

      # RabbitMQ
      RABBITMQ_URL: amqp://guest:guest@rabbitmq:5672/
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
    networks:
      - dreon-notification-network

  postgres:
    image: postgres:17-alpine
    container_name: dreon-notification-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: dreon-notification
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - dreon-notification-network

  redis:
    image: redis:latest
    container_name: dreon-notification-redis
    restart: unless-stopped
    command: redis-server --appendonly yes
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - dreon-notification-network

  rabbitmq:
    image: rabbitmq:3-management-alpine
    container_name: dreon-notification-rabbitmq
    restart: unless-stopped
    ports:
      - "5672:5672"
      - "15672:15672"
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "check_running"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - dreon-notification-network

volumes:
  postgres_data:
    driver: local
  redis_data:
    driver: local

networks:
  dreon-notification-network:
    driver: bridge
